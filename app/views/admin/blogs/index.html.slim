.form-group
  .col-sm-2
    = link_to _('Create Blog'), new_admin_blog_path, class: 'btn btn-success pull-left'
  .col-sm-10
    = render 'form_search'
table.table.table-bordered.table-hover.table-condensed.table-responsive
  thead
    tr
      th Image
      th = sort_link(@q, :title)
      th Description
      th Tags
      th Action
  tbody
    - @blogs.each do |blog|
      tr
        th = image_tag blog.image.url(:mini)
        td.title = blog.title
        td = blog.description
        td = blog.tag_list
        td.text-center
          .btn-group
            = link_to edit_admin_blog_path(blog), class: 'btn btn-warning btn-md'
              span.glyphicon.glyphicon-edit
            = link_to admin_blog_path(blog), class: 'btn btn-danger btn-md', method: :delete,
              data: { confirm: _('Are you sure?') }
              span.glyphicon.glyphicon-remove
